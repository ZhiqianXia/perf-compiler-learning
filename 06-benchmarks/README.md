# Benchmarks

Store benchmark inputs, commands, and result snapshots here.

Recommended split:

- `cpp/`: numerical and algorithm microbenchmarks
- `cuda/`: kernel latency and throughput results
- `llvm/`: pass-related measurement notes and before/after comparisons
