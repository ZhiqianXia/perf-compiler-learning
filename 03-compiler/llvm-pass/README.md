# LLVM Pass

Start with analysis passes, then move to transformation passes.

Suggested progression:

1. hello pass
2. opcode counter pass
3. loop analysis pass
4. instrumentation pass
5. simple transform pass

Each pass directory should include build commands, `opt` usage, and a tiny IR or C++ example for verification.
