# Compiler

This directory is for LLVM experiments, including analysis passes, transformation passes, and IR notes.

Each pass should document:

- what it inspects or transforms
- which LLVM APIs it relies on
- how to build and run it locally
- how to validate the output IR
